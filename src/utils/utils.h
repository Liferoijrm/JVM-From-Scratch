/**
 * @file utils.h
 * @brief Definição de tipos padronizados de tamanhos fixos para uso no projeto.
 */

#ifndef UTILS_H
#define UTILS_H

#include <stdint.h>

/**
 * @def u1
 * @brief Tipo inteiro sem sinal de 1 byte (8 bits).
 */
#define u1 uint8_t

/**
 * @def u2
 * @brief Tipo inteiro sem sinal de 2 bytes (16 bits).
 */
#define u2 uint16_t

/**
 * @def u4
 * @brief Tipo inteiro sem sinal de 4 bytes (32 bits).
 */
#define u4 uint32_t

#endif